import { getScratch } from "./utils";

export function visualProcess(cyInstance, dimension) {
    cyInstance.startBatch();
    initNodeColors( cyInstance, dimension);
}


function initNodeColors(cyInstance, dimension){


}
